@extends('customer.layout')
@section('title','Wishlist')
@section('content')
<section class="customer-card"><div class="d-flex justify-content-between align-items-center mb-3"><h2 class="mb-0">My Wishlist</h2><a class="customer-action" href="/products">Continue shopping</a></div><div class="wishlist-grid">@forelse($items as $item)<article class="wishlist-product">@if($item->product?->thumbnail)<img src="{{ $item->product->thumbnail->url }}" alt="{{ $item->product->name }}">@else<div class="wishlist-placeholder"><i class="bi bi-image"></i></div>@endif<div><b>{{ $item->product?->name }}</b><span>${{ number_format($item->product?->selling_price ?? 0,2) }}</span><small class="text-muted">{{ $item->product?->stock_quantity ? 'In stock' : 'Out of stock' }}</small></div><form method="POST" action="{{ route('customer.wishlist.remove',$item->product_id) }}">@csrf @method('DELETE')<button class="customer-action danger" title="Remove from wishlist"><i class="bi bi-trash"></i> Remove</button></form></article>@empty<div class="customer-empty"><i class="bi bi-heart"></i><p>Your wishlist is empty.</p><a class="customer-action primary mt-3" href="/products">Browse products</a></div>@endforelse</div><div class="mt-3">{{ $items->links() }}</div></section>
@endsection
