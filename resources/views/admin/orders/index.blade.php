@extends('layouts.admin')
@section('title','Ecommerce Orders')
@section('content')
<div class="content-heading mb-4"><h1>Ecommerce Orders</h1><p>Orders placed from your storefront will appear here for fulfilment.</p></div>
<div class="panel"><div class="d-flex flex-wrap gap-2 mb-4">@php($labels=['pending'=>'Pending','accepted'=>'Accepted','processing'=>'Processing','shipped'=>'Shipped','delivered'=>'Delivered','cancelled'=>'Cancelled'])<a class="status-filter {{ !$selectedStatus ? 'active' : '' }}" href="{{ route('admin.orders.index') }}">All</a>@foreach($statuses as $status)<a class="status-filter {{ $selectedStatus === $status ? 'active' : '' }}" href="{{ route('admin.orders.index',['status'=>$status]) }}">{{ $labels[$status] }}</a>@endforeach</div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Order ID</th><th>Order date</th><th>Customer</th><th>Warehouse</th><th>Total amount</th><th>Payment</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($orders as $order)<tr><td class="fw-bold">{{ $order->order_number }}</td><td>{{ $order->created_at->format('d M Y, h:i A') }}</td><td>{{ $order->customer_name }}@if($order->customer_phone)<br><small class="text-muted">{{ $order->customer_phone }}</small>@endif</td><td>{{ $order->warehouse?->name ?: 'Unassigned' }}</td><td class="fw-semibold">${{ number_format($order->total,2) }}</td><td>{{ $order->payment_method }}<br><small class="text-muted text-capitalize">{{ $order->payment_status }}</small></td><td><span class="order-status {{ $order->status }}">{{ $labels[$order->status] }}</span></td><td class="text-end"><a class="action-button edit" href="{{ route('admin.orders.show',$order) }}"><i class="bi bi-eye me-1"></i>Details</a></td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted"><i class="bi bi-cart-x fs-2 d-block mb-2"></i>No ecommerce orders yet.<br><small>Once the storefront is connected, customer orders will arrive here.</small></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>
@endsection
