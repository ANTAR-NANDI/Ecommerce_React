@extends('layouts.admin')
@section('title','Invoice '.$order->order_number)
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4 no-print"><div class="content-heading"><h1>Ecommerce Invoice</h1><p>{{ $order->order_number }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.orders.show',$order) }}" class="btn btn-outline-secondary">Order details</a><button type="button" class="btn text-white" style="background:var(--brand)" onclick="window.print()"><i class="bi bi-printer me-1"></i>Print invoice</button></div></div><div class="panel invoice-sheet mx-auto"><div class="d-flex justify-content-between border-bottom pb-4 mb-4"><div><div class="brand-name fs-4">EBay</div><div class="text-muted small mt-2">Ecommerce order invoice</div></div><div class="text-end"><div class="fw-bold fs-5">{{ $order->order_number }}</div><div class="text-muted small">{{ $order->created_at->format('d M Y, h:i A') }}</div></div></div><div class="row mb-4"><div class="col-6"><div class="text-muted small text-uppercase">Customer</div><div class="fw-semibold">{{ $order->customer_name }}</div><div class="small">{{ $order->customer_phone }}</div><div class="small">{{ $order->customer_email }}</div></div><div class="col-6 text-end"><div class="text-muted small text-uppercase">Delivery</div><div>{{ $order->warehouse?->name ?: 'Warehouse pending' }}</div><div>{{ ucfirst($order->status) }}</div><div>{{ $order->payment_method }}</div></div></div><table class="table"><thead><tr><th>Product</th><th class="text-center">Qty</th><th class="text-end">Price</th><th class="text-end">Amount</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td class="text-center">{{ rtrim(rtrim(number_format($item->quantity,2),'0'),'.') }}</td><td class="text-end">${{ number_format($item->unit_price,2) }}</td><td class="text-end fw-semibold">${{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><td colspan="3" class="text-end">Subtotal</td><td class="text-end">${{ number_format($order->subtotal,2) }}</td></tr><tr><td colspan="3" class="text-end">Shipping</td><td class="text-end">${{ number_format($order->shipping_charge,2) }}</td></tr><tr><th colspan="3" class="text-end">Total</th><th class="text-end fs-5">${{ number_format($order->total,2) }}</th></tr></tfoot></table><div class="border-top pt-3 text-center text-muted small">Thank you for shopping with us.</div></div>
@endsection
