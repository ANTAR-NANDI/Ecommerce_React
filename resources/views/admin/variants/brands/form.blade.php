@extends('layouts.admin')
@php($editing=$brand->exists)
@section('title',$editing ? 'Edit Brand' : 'Add Brand')
@section('content')
<div class="content-heading mb-4"><h1>{{ $editing ? 'Edit brand' : 'Add brand' }}</h1><p>Add a brand name, icon, and visibility status.</p></div><form method="POST" action="{{ $editing ? route('admin.brands.update',$brand) : route('admin.brands.store') }}">@csrf @if($editing) @method('PUT') @endif<div class="row g-4"><div class="col-lg-7"><div class="panel"><label class="form-label">Brand name</label><input name="name" value="{{ old('name',$brand->name) }}" class="form-control" required>@error('name')<div class="text-danger small">{{ $message }}</div>@enderror<div class="mt-4"><label class="form-label">Status</label><label class="form-check ms-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$brand->is_active))> Active</label></div></div></div><div class="col-lg-5"><div class="panel"><label class="form-label">Brand icon</label><input id="brand-icon-media" type="hidden" name="icon_media_id" value="{{ old('icon_media_id',$brand->icon_media_id) }}"><div class="media-preview mb-2" data-media-preview="brand-icon-media">@if($brand->icon)<img src="{{ $brand->icon->url }}" alt="">@else <span>No icon selected</span>@endif</div><button type="button" class="btn border w-100" data-media-picker-target="brand-icon-media"><i class="bi bi-images me-1"></i><span data-media-label="brand-icon-media">{{ $brand->icon?->original_name ?? 'Choose icon' }}</span></button></div></div><div class="col-12"><button class="btn text-white" style="background:var(--brand)">{{ $editing ? 'Update brand' : 'Save brand' }}</button></div></div></form>
@endsection
