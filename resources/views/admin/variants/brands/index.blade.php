@extends('layouts.admin')
@section('title','Brands')
@section('content')
<div class="d-flex justify-content-between align-items-center content-heading mb-4"><div><h1>Brands</h1><p>Manage product brands and their visual icons.</p></div><a href="{{ route('admin.brands.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add brand</a></div><div class="panel p-0 overflow-hidden"><table class="table mb-0"><thead><tr><th>Brand</th><th>Icon</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($brands as $brand)<tr><td class="fw-semibold">{{ $brand->name }}</td><td>@if($brand->icon)<img src="{{ $brand->icon->url }}" class="media-select-thumb" alt="">@else — @endif</td><td><span class="badge {{ $brand->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $brand->is_active ? 'Active' : 'Inactive' }}</span></td><td class="text-end"><a class="action-button edit" href="{{ route('admin.brands.edit',$brand) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.brands.destroy',$brand) }}" onsubmit="return confirm('Delete this brand?')">@csrf @method('DELETE')<button class="action-button delete">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="text-center py-5">No brands yet.</td></tr>@endforelse</tbody></table></div>
@endsection
