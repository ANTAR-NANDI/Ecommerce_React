@extends('layouts.admin')
@php($editing=$color->exists)
@section('title',$editing ? 'Edit Color' : 'Add Color')
@section('content')
<div class="content-heading mb-4"><h1>{{ $editing ? 'Edit color' : 'Add color' }}</h1><p>Select a color visually; its HEX code is generated automatically.</p></div><form method="POST" action="{{ $editing ? route('admin.colors.update',$color) : route('admin.colors.store') }}">@csrf @if($editing) @method('PUT') @endif<div class="panel" style="max-width:700px"><div class="row g-3"><div class="col-md-6"><label class="form-label">Color name</label><input name="name" value="{{ old('name',$color->name) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Pick color</label><input type="color" value="{{ old('hex_code',$color->hex_code) }}" class="form-control form-control-color w-100" data-color-picker="#color-code"></div><div class="col-md-3"><label class="form-label">Color code</label><input id="color-code" name="hex_code" value="{{ old('hex_code',$color->hex_code) }}" class="form-control" required></div><div class="col-12"><label class="form-check"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$color->is_active))> Active</label></div><div class="col-12"><button class="btn text-white" style="background:var(--brand)">{{ $editing ? 'Update color' : 'Save color' }}</button></div></div></div></form>
@endsection
