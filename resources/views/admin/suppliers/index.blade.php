@extends('layouts.admin')
@section('title','Suppliers')
@section('content')
<div class="d-flex justify-content-between align-items-center content-heading mb-4"><div><h1>Suppliers</h1><p>Manage vendors used for warehouse purchases.</p></div><a href="{{ route('admin.suppliers.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add supplier</a></div><div class="panel p-0 overflow-hidden"><table class="table mb-0"><thead><tr><th>Supplier</th><th>Photo</th><th>Phone</th><th>Email</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($suppliers as $supplier)<tr><td class="fw-semibold">{{ $supplier->name }}</td><td>@if($supplier->photo)<img src="{{ $supplier->photo->url }}" class="media-select-thumb">@else — @endif</td><td>{{ $supplier->phone }}</td><td>{{ $supplier->email ?: '—' }}</td><td><span class="badge {{ $supplier->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $supplier->is_active?'Active':'Inactive' }}</span></td><td class="text-end"><a class="action-button edit" href="{{ route('admin.suppliers.edit',$supplier) }}">Edit</a><form method="POST" class="d-inline" action="{{ route('admin.suppliers.destroy',$supplier) }}" onsubmit="return confirm('Delete this supplier?')">@csrf @method('DELETE')<button class="action-button delete">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5">No suppliers yet.</td></tr>@endforelse</tbody></table></div>
@endsection
