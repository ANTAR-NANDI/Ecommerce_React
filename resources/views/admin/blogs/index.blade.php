@extends('layouts.admin')
@section('title','Blogs')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 content-heading mb-4"><div><h1>Blog</h1><p>Create and manage articles for the ecommerce storefront.</p></div><a href="{{ route('admin.blogs.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add blog</a></div>
<div class="panel mb-4"><form method="GET" class="row g-2"><div class="col-md-5"><input name="search" value="{{ $search }}" class="form-control" placeholder="Search blog title"></div><div class="col-auto"><button class="btn btn-outline-secondary h-100"><i class="bi bi-search"></i> Search</button></div>@if($search)<div class="col-auto"><a class="btn btn-light h-100" href="{{ route('admin.blogs.index') }}">Clear</a></div>@endif</form></div>
<div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Post</th><th>Category</th><th>Tags</th><th>Status</th><th>Published</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($blogs as $blog)<tr><td><div class="d-flex align-items-center gap-3">@if($blog->thumbnail)<img class="blog-list-image" src="{{ $blog->thumbnail->url }}" alt="{{ $blog->title }}">@else<div class="blog-list-image"><i class="bi bi-image"></i></div>@endif<div><div class="fw-semibold">{{ $blog->title }}</div><small class="text-muted">/{{ $blog->slug }}</small></div></div></td><td>{{ $blog->category?->name ?: 'Uncategorized' }}</td><td>@forelse($blog->tags ?? [] as $tag)<span class="badge text-bg-light border me-1">{{ $tag }}</span>@empty<span class="text-muted">—</span>@endforelse</td><td><span class="order-status {{ $blog->status === 'published' ? 'delivered' : 'pending' }}">{{ ucfirst($blog->status) }}</span></td><td>{{ $blog->published_at?->format('d M Y') ?: '—' }}</td><td class="text-end"><a href="{{ route('admin.blogs.edit',$blog) }}" class="action-button edit"><i class="bi bi-pencil-square me-1"></i>Edit</a><form class="d-inline" method="POST" action="{{ route('admin.blogs.destroy',$blog) }}" onsubmit="return confirm('Delete this blog post?')">@csrf @method('DELETE')<button class="action-button delete"><i class="bi bi-trash3 me-1"></i>Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5"><i class="bi bi-journal-text fs-2 d-block mb-2"></i>No blog posts yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $blogs->links() }}</div>
@endsection
