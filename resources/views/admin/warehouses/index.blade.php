@extends('layouts.admin')
@section('title','Warehouses')
@section('content')
<div class="d-flex justify-content-between align-items-center content-heading mb-4"><div><h1>Warehouses</h1><p>ERP locations that will own and track inventory.</p></div><a href="{{ route('admin.warehouses.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add warehouse</a></div><div class="panel p-0 overflow-hidden"><table class="table mb-0"><thead><tr><th>Warehouse</th><th>Code</th><th>Manager</th><th>Contact</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($warehouses as $warehouse)<tr><td class="fw-semibold">{{ $warehouse->name }}</td><td>{{ $warehouse->code }}</td><td>{{ $warehouse->manager_name ?: '—' }}</td><td>{{ $warehouse->phone ?: $warehouse->email ?: '—' }}</td><td><span class="badge {{ $warehouse->is_active ? 'text-bg-success':'text-bg-secondary' }}">{{ $warehouse->is_active?'Active':'Inactive' }}</span></td><td class="text-end"><a class="action-button edit" href="{{ route('admin.warehouses.edit',$warehouse) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.warehouses.destroy',$warehouse) }}" onsubmit="return confirm('Delete this warehouse?')">@csrf @method('DELETE')<button class="action-button delete">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5">No warehouses yet.</td></tr>@endforelse</tbody></table></div>
@endsection
