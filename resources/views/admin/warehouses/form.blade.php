@extends('layouts.admin')
@php($editing=$warehouse->exists)
@section('title',$editing?'Edit Warehouse':'Add Warehouse')
@section('content')
<div class="content-heading mb-4"><h1>{{ $editing?'Edit warehouse':'Add warehouse' }}</h1><p>Create the warehouse and its own Warehouse Admin login in one step.</p></div>@php($warehouseAdmin=$editing ? $warehouse->users->firstWhere('role','warehouse_admin') : null)<form method="POST" action="{{ $editing?route('admin.warehouses.update',$warehouse):route('admin.warehouses.store') }}">@csrf @if($editing) @method('PUT') @endif<div class="panel mb-4"><div class="panel-heading mb-3">Warehouse details</div><div class="row g-3"><div class="col-md-6"><label class="form-label">Warehouse name</label><input name="name" value="{{ old('name',$warehouse->name) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Warehouse code</label><input name="code" value="{{ old('code',$warehouse->code) }}" class="form-control" placeholder="e.g. DHAKA-01" required></div><div class="col-md-4"><label class="form-label">Manager / contact person</label><input name="manager_name" value="{{ old('manager_name',$warehouse->manager_name) }}" class="form-control"></div><div class="col-md-4"><label class="form-label">Phone</label><input name="phone" value="{{ old('phone',$warehouse->phone) }}" class="form-control"></div><div class="col-md-4"><label class="form-label">Warehouse email</label><input type="email" name="email" value="{{ old('email',$warehouse->email) }}" class="form-control"></div><div class="col-12"><label class="form-label">Address</label><textarea name="address" class="form-control" rows="3">{{ old('address',$warehouse->address) }}</textarea></div></div></div><div class="panel"><div class="panel-heading mb-1">Warehouse Admin login</div><p class="text-muted small mb-3">This account can access only this warehouse’s stock, ecommerce orders, and POS.</p><div class="row g-3"><div class="col-md-6"><label class="form-label">Login email</label><input type="email" name="login_email" value="{{ old('login_email',$warehouseAdmin?->email) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">{{ $editing ? 'New password (leave empty to keep current)' : 'Password' }}</label><input type="password" name="login_password" class="form-control" {{ $editing ? '' : 'required' }}></div><div class="col-12"><label class="form-check"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$warehouse->is_active))> Active warehouse</label></div><div class="col-12"><button class="btn text-white" style="background:var(--brand)">{{ $editing?'Update warehouse':'Create warehouse & login' }}</button></div></div></div></form>
@endsection
