@extends('layouts.admin')
@section('title','POS Drafts')
@section('content')
<div class="content-heading mb-4"><h1>POS Drafts</h1><p>Held carts can be continued and completed whenever the customer is ready.</p></div>
<div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Draft reference</th><th>Saved</th><th>Customer</th><th>Warehouse</th><th>Items</th><th class="text-end">Total</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($orders as $order)<tr><td class="fw-bold">{{ $order->order_number }}</td><td>{{ $order->created_at->format('d M Y, h:i A') }}</td><td>{{ $order->customer_name ?: 'Walk-in customer' }}@if($order->customer_phone)<br><small class="text-muted">{{ $order->customer_phone }}</small>@endif</td><td>{{ $order->warehouse?->name ?: '—' }}</td><td>{{ $order->items->sum('quantity') }}</td><td class="text-end fw-bold">${{ number_format($order->total,2) }}</td><td class="text-end"><a class="btn btn-sm text-white" style="background:var(--brand)" href="{{ route('admin.pos.drafts.continue',$order) }}"><i class="bi bi-play-fill me-1"></i>Continue</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted"><i class="bi bi-pause-circle fs-3 d-block mb-2"></i>There are no held POS carts.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>
@endsection
