@extends('layouts.admin')
@section('title','POS Invoice')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4 no-print"><div class="content-heading"><h1>POS Invoice</h1><p>{{ $order->order_number }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.pos.history') }}" class="btn btn-outline-secondary">Back to sales</a><button type="button" onclick="window.print()" class="btn text-white" style="background:var(--brand)"><i class="bi bi-printer me-1"></i>Print invoice</button></div></div>
<div class="panel invoice-sheet mx-auto"><div class="d-flex justify-content-between border-bottom pb-4 mb-4"><div><div class="brand-name fs-4">EBay</div><div class="text-muted small mt-2">Point of Sale Invoice</div></div><div class="text-end"><div class="fw-bold fs-5">{{ $order->order_number }}</div><div class="text-muted small">{{ $order->created_at->format('d M Y, h:i A') }}</div></div></div><div class="row mb-4"><div class="col-6"><div class="text-muted small text-uppercase">Customer</div><div class="fw-semibold">{{ $order->customer_name ?: 'Walk-in customer' }}</div>@if($order->customer_phone)<div class="small">{{ $order->customer_phone }}</div>@endif</div><div class="col-6 text-end"><div class="text-muted small text-uppercase">Sale details</div><div>Warehouse: {{ $order->warehouse?->name ?: '—' }}</div><div>Payment: {{ $order->payment_method ?: 'Cash' }}</div></div></div><table class="table"><thead><tr><th>Product</th><th class="text-center">Qty</th><th class="text-end">Price</th><th class="text-end">Amount</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td class="text-center">{{ rtrim(rtrim(number_format($item->quantity,2),'0'),'.') }}</td><td class="text-end">${{ number_format($item->unit_price,2) }}</td><td class="text-end fw-semibold">${{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="3" class="text-end">Total</th><th class="text-end fs-5">${{ number_format($order->total,2) }}</th></tr></tfoot></table><div class="border-top pt-3 text-center text-muted small">Thank you for shopping with us.</div></div>
@endsection
