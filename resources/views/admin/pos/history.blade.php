@extends('layouts.admin')
@section('title','POS Sales History')
@section('content')
<div class="d-flex align-items-center justify-content-between mb-4"><div class="content-heading"><h1>POS Sales History</h1><p>Completed counter sales and their printable invoices.</p></div><a class="btn text-white" style="background:var(--brand)" href="{{ route('admin.pos.index') }}"><i class="bi bi-plus-lg me-1"></i>New sale</a></div>
<div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Invoice</th><th>Date</th><th>Customer</th><th>Warehouse</th><th>Items</th><th>Payment</th><th class="text-end">Total</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><span class="fw-bold">{{ $order->order_number }}</span></td><td>{{ $order->created_at->format('d M Y') }}<br><small class="text-muted">{{ $order->created_at->format('h:i A') }}</small></td><td>{{ $order->customer_name ?: 'Walk-in customer' }}<br>@if($order->customer_phone)<small class="text-muted">{{ $order->customer_phone }}</small>@endif</td><td>{{ $order->warehouse?->name ?: '—' }}</td><td>{{ $order->items->sum('quantity') }}</td><td><span class="badge text-bg-light border">{{ $order->payment_method ?: 'Cash' }}</span></td><td class="text-end fw-bold">${{ number_format($order->total,2) }}</td><td class="text-end"><a class="action-button edit" href="{{ route('admin.pos.invoice',$order) }}"><i class="bi bi-receipt me-1"></i>Invoice</a></td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted"><i class="bi bi-receipt-cutoff fs-3 d-block mb-2"></i>No completed POS sales yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>
@endsection
