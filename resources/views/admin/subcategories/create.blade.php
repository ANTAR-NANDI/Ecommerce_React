@extends('layouts.admin')
@section('title', 'Add Subcategory')
@section('content')
<div class="d-flex flex-wrap gap-3 justify-content-between align-items-center content-heading mb-4"><div><h1>Add subcategory</h1><p>Attach a detailed collection to one or more categories.</p></div><a href="{{ route('admin.subcategories.index') }}" class="btn border px-3 py-2" style="border-color:var(--line)!important">All subcategories</a></div>
<form method="POST" action="{{ route('admin.subcategories.store') }}"><div class="row g-4"><div class="col-lg-8"><div class="panel"><div class="row g-3"><div class="col-12"><label class="form-label">Subcategory name</label><input name="name" value="{{ old('name') }}" class="form-control @error('name') is-invalid @enderror" placeholder="e.g. Mobile accessories" required>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-12"><label class="form-label">Categories</label><select name="categories[]" class="js-category-select @error('categories') is-invalid @enderror" multiple required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(in_array($category->id, old('categories', [])))>{{ $category->name }}</option>@endforeach</select><div class="form-text">Search and choose one or more parent categories.</div>@error('categories')<div class="text-danger small mt-1">{{ $message }}</div>@enderror</div><div class="col-12"><label class="form-label">Short description <span class="fw-normal" style="color:var(--muted)">(optional)</span></label><textarea name="short_description" class="form-control" rows="4" maxlength="255" placeholder="A concise description for the storefront.">{{ old('short_description') }}</textarea></div></div></div></div><div class="col-lg-4"><div class="panel"><div class="d-flex justify-content-between align-items-center mb-3"><div class="panel-heading">Media</div><a href="{{ route('admin.media.index') }}" class="small text-decoration-none" style="color:var(--brand)">Open library</a></div><label class="form-label">Icon <span class="fw-normal" style="color:var(--muted)">(optional)</span></label><select name="icon_media_id" class="form-select"><option value="">No icon selected</option>@foreach($media as $item)<option value="{{ $item->id }}">{{ $item->original_name }}</option>@endforeach</select><p class="small mt-3 mb-0" style="color:var(--muted)">For best results, use a square image with a clear subject.</p></div></div><div class="col-12"><button class="btn text-white px-4 py-2" style="background:var(--brand);border-radius:10px"><i class="bi bi-check2 me-1"></i>Save subcategory</button></div></div>@csrf</form>
@endsection
