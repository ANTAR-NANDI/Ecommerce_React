@extends('layouts.admin')
@section('title','Purchases')
@section('content')
<div class="d-flex justify-content-between align-items-center content-heading mb-4"><div><h1>Purchases</h1><p>Warehouse-specific purchasing and supplier receiving.</p></div><a href="{{ route('admin.purchases.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>New purchase</a></div><div class="panel p-0 overflow-hidden"><table class="table mb-0"><thead><tr><th>Purchase no.</th><th>Date</th><th>Warehouse</th><th>Supplier</th><th>Items</th><th>Total</th><th>Status</th><th class="text-end">Invoice</th></tr></thead><tbody>@forelse($purchases as $purchase)<tr><td class="fw-semibold">{{ $purchase->purchase_number }}</td><td>{{ $purchase->purchase_date->format('d M Y') }}</td><td>{{ $purchase->warehouse->name }}</td><td>{{ $purchase->supplier_name }}</td><td>{{ $purchase->items()->count() }}</td><td>${{ number_format($purchase->total,2) }}</td><td><span class="badge text-bg-{{ $purchase->status==='received'?'success':'secondary' }}">{{ ucfirst($purchase->status) }}</span></td><td class="text-end"><a href="{{ route('admin.purchases.invoice',$purchase) }}" class="btn btn-sm btn-outline-secondary"><i class="bi bi-receipt"></i><span class="visually-hidden">View invoice</span></a></td></tr>@empty<tr><td colspan="8" class="text-center py-5">No purchases yet. Record your first warehouse purchase.</td></tr>@endforelse</tbody></table></div>
@endsection
