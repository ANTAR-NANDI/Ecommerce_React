@extends('layouts.admin')
@section('title','Menus')
@section('content')
<div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4"><div class="content-heading"><h1>Menus</h1><p>Configure the live navigation and footer links. Use order numbers to control placement.</p></div><a href="{{ route('admin.cms.menus.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add Menu</a></div>
@php($locations=['header'=>'Header navigation','footer_shop'=>'Footer · Shop','footer_support'=>'Footer · Customer service','footer_company'=>'Footer · Company'])
<div class="row g-4">@foreach($locations as $key=>$title)<div class="col-lg-6"><section class="panel h-100"><h2 class="h5">{{ $title }}</h2><p class="small text-muted">Enabled items appear live in the selected area.</p><div class="list-group list-group-flush">@forelse($menus->where('location',$key) as $menu)<div class="list-group-item px-0 d-flex align-items-center gap-2"><i class="bi bi-grip-vertical text-muted"></i><div class="flex-grow-1"><b>{{ $menu->label }}</b><div class="small text-muted">{{ $menu->url }} · Order {{ $menu->sort_order }} · {{ $menu->is_active ? 'Enabled' : 'Disabled' }}</div></div><a class="action-button edit" href="{{ route('admin.cms.menus.edit',$menu) }}"><i class="bi bi-pencil-square"></i></a><form method="POST" action="{{ route('admin.cms.menus.destroy',$menu) }}" onsubmit="return confirm('Delete this menu item?')">@csrf @method('DELETE')<button class="action-button delete"><i class="bi bi-trash3"></i></button></form></div>@empty<div class="py-4 text-muted small">No menu items assigned here.</div>@endforelse</div></section></div>@endforeach</div>
@endsection
