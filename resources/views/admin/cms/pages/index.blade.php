@extends('layouts.admin')
@section('title','CMS Pages')
@section('content')
<div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4"><div class="content-heading"><h1>Pages</h1><p>Create policy, information, and custom storefront pages.</p></div><a href="{{ route('admin.cms.pages.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-plus-lg me-1"></i>Add Page</a></div>
<div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Page</th><th>Public URL</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($pages as $page)<tr><td><b>{{ $page->title }}</b><div class="small text-muted">{{ Str::limit($page->meta_description, 70) }}</div></td><td><code>/page/{{ $page->slug }}</code></td><td><span class="order-status {{ $page->is_active ? 'delivered' : 'cancelled' }}">{{ $page->is_active ? 'Published' : 'Draft' }}</span></td><td class="text-end"><a class="action-button" target="_blank" href="{{ url('/page/'.$page->slug) }}"><i class="bi bi-eye"></i></a><a class="action-button edit" href="{{ route('admin.cms.pages.edit',$page) }}"><i class="bi bi-pencil-square"></i></a><form class="d-inline" method="POST" action="{{ route('admin.cms.pages.destroy',$page) }}" onsubmit="return confirm('Delete this page?')">@csrf @method('DELETE')<button class="action-button delete"><i class="bi bi-trash3"></i></button></form></td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-muted">No custom pages yet. Create your first page.</td></tr>@endforelse</tbody></table></div>@if($pages->hasPages())<div class="p-3">{{ $pages->links() }}</div>@endif</div>
@endsection
