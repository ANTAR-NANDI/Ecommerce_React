@extends('layouts.admin')
@section('title', 'Add Category')
@section('content')
<div class="d-flex flex-wrap gap-3 justify-content-between align-items-center content-heading mb-4"><div><h1>Add category</h1><p>Create a main storefront collection.</p></div><a href="{{ route('admin.categories.index') }}" class="btn border px-3 py-2" style="border-color:var(--line)!important">All categories</a></div>
<form method="POST" action="{{ route('admin.categories.store') }}"><div class="row g-4"><div class="col-lg-8"><div class="panel"><div class="row g-3"><div class="col-md-8"><label class="form-label">Category name</label><input name="name" value="{{ old('name') }}" class="form-control @error('name') is-invalid @enderror" placeholder="e.g. Electronics" required>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-4"><label class="form-label">Display order</label><input type="number" min="0" name="display_order" value="{{ old('display_order', 0) }}" class="form-control @error('display_order') is-invalid @enderror" required></div><div class="col-12"><label class="form-label">Status</label><div class="d-flex gap-4"><label class="form-check"><input class="form-check-input" type="radio" name="is_active" value="1" checked><span class="form-check-label">Active</span></label><label class="form-check"><input class="form-check-input" type="radio" name="is_active" value="0" {{ old('is_active') === '0' ? 'checked' : '' }}><span class="form-check-label">Inactive</span></label></div></div></div></div></div><div class="col-lg-4"><div class="panel"><div class="d-flex justify-content-between align-items-center mb-3"><div class="panel-heading">Media</div><a href="{{ route('admin.media.index') }}" class="small text-decoration-none" style="color:var(--brand)">Open library</a></div><label class="form-label">Icon</label><select name="icon_media_id" class="form-select mb-3"><option value="">No icon selected</option>@foreach($media as $item)<option value="{{ $item->id }}">{{ $item->original_name }}</option>@endforeach</select><label class="form-label">Banner</label><select name="banner_media_id" class="form-select"><option value="">No banner selected</option>@foreach($media as $item)<option value="{{ $item->id }}">{{ $item->original_name }}</option>@endforeach</select><p class="small mt-3 mb-0" style="color:var(--muted)">Upload assets once in Media Library, then reuse them anywhere.</p></div></div><div class="col-12"><button class="btn text-white px-4 py-2" style="background:var(--brand);border-radius:10px"><i class="bi bi-check2 me-1"></i>Save category</button></div></div>@csrf</form>
@endsection
