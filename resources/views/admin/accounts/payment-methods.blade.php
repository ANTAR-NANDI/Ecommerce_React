@extends('layouts.admin')
@section('title','Payment Methods')
@section('content')
<div class="content-heading mb-4"><h1>Payment Methods</h1><p>Cash in Hand is fixed. New methods create an account head under Bank Accounts.</p></div><div class="row g-4"><div class="col-lg-4"><form method="POST" action="{{ route('admin.accounts.payment-methods.store') }}" class="panel">@csrf<h5 class="mb-3">Add payment method</h5><label class="form-label">Payment method name</label><input class="form-control" name="name" placeholder="e.g. bKash, Nagad, City Bank" required><button class="btn text-white mt-3" style="background:var(--brand)">Save</button></form></div><div class="col-lg-8"><div class="panel p-0 overflow-hidden"><table class="table mb-0"><thead><tr><th>Payment method</th><th>Account head</th><th>Parent</th><th>Type</th></tr></thead><tbody>@foreach($methods as $method)<tr><td><strong>{{ $method->name }}</strong></td><td>{{ $method->account?->code ?? 'Unmapped' }} — {{ $method->account?->head_name ?? 'Account pending' }}</td><td>{{ $method->is_fixed ? 'Current Assets' : 'Bank Accounts' }}</td><td>{!! $method->is_fixed ? '<span class="badge text-bg-secondary">Fixed</span>' : '<span class="badge text-bg-success">Custom</span>' !!}</td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ $methods->links() }}</div></div></div>
@endsection
