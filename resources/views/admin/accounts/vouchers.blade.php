@extends('layouts.admin')
@section('title', $voucherLabel)
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 content-heading mb-4"><div><h1>{{ $voucherLabel }}</h1><p>Post and review {{ strtolower($voucherLabel) }} entries.</p></div><button class="btn text-white" style="background:var(--brand)" data-bs-toggle="collapse" data-bs-target="#voucher-form"><i class="bi bi-plus-circle me-1"></i>Create {{ $voucherLabel }}</button></div>
<div id="voucher-form" class="collapse mb-4"><form method="POST" action="{{ route('admin.accounts.vouchers.store') }}" class="panel">@csrf<input type="hidden" name="voucher_type" value="{{ $voucherType }}"><div class="row g-3"><div class="col-md-4"><label class="form-label">Date</label><input type="date" class="form-control" name="transaction_date" value="{{ now()->toDateString() }}" required></div><div class="col-md-4"><label class="form-label">Amount</label><input class="form-control" type="number" min=".01" step=".01" name="amount" required></div><div class="col-md-4"><label class="form-label">Narration</label><input class="form-control" name="ledger_comment" placeholder="Ledger comment"></div><div class="col-md-6"><label class="form-label">Debit account</label><select class="form-select" name="debit_account_id" required><option value="">Choose debit account</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->head_name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Credit account</label><select class="form-select" name="credit_account_id" required><option value="">Choose credit account</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->head_name }}</option>@endforeach</select></div>@if($voucherType !== 'journal')<div class="col-md-4"><label class="form-label">Supplier</label><select class="form-select" name="supplier_id"><option value="">None</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Customer</label><select class="form-select" name="customer_id"><option value="">None</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->full_name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Purchase reference</label><select class="form-select" name="purchase_id"><option value="">None</option>@foreach($purchases as $purchase)<option value="{{ $purchase->id }}">{{ $purchase->purchase_number }}</option>@endforeach</select></div>@endif</div><button class="btn text-white mt-4" style="background:var(--brand)">Save {{ $voucherLabel }}</button></form></div>
<div class="panel p-0 overflow-hidden"><div class="p-3 border-bottom d-flex justify-content-between"><strong>{{ $voucherLabel }} list</strong><input id="voucher-search" class="form-control form-control-sm" style="max-width:230px" placeholder="Search"></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>SL No</th><th>VNo</th><th>Date</th><th>Account name</th><th>Ledger comment</th><th>Debit</th><th>Credit</th><th>Reverse account</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr data-voucher><td>{{ $transactions->firstItem()+$loop->index }}</td><td><code>{{ $transaction->voucher_no }}</code></td><td>{{ $transaction->transaction_date->format('d-m-Y') }}</td><td>{{ $transaction->account->head_name }}</td><td>{{ $transaction->ledger_comment }}</td><td>{{ $transaction->entry_type==='debit' ? 'BDT '.number_format($transaction->amount,2) : 'BDT 0.00' }}</td><td>{{ $transaction->entry_type==='credit' ? 'BDT '.number_format($transaction->amount,2) : 'BDT 0.00' }}</td><td>—</td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">No {{ strtolower($voucherLabel) }} entries yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $transactions->links() }}</div><script>document.querySelector('#voucher-search')?.addEventListener('input',e=>document.querySelectorAll('[data-voucher]').forEach(r=>r.hidden=!r.textContent.toLowerCase().includes(e.target.value.toLowerCase())));</script>
@endsection
