@extends('layouts.admin')
@section('title','Accounting Vouchers')
@section('content')
<div class="content-heading mb-4"><h1>Accounting vouchers</h1><p>Every voucher posts one debit and one credit entry, keeping the general ledger balanced.</p></div>
<div class="row g-4"><div class="col-xl-5"><form method="POST" action="{{ route('admin.accounts.vouchers.store') }}" class="panel">@csrf<h5 class="mb-3">Post voucher</h5><div class="row g-3"><div class="col-md-6"><label class="form-label">Voucher type</label><select class="form-select" name="voucher_type">@foreach(['debit'=>'Debit voucher','credit'=>'Credit voucher','contra'=>'Contra voucher','journal'=>'Journal voucher'] as $key=>$label)<option value="{{ $key }}" @selected(old('voucher_type')===$key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Date</label><input type="date" class="form-control" name="transaction_date" value="{{ old('transaction_date',now()->toDateString()) }}" required></div><div class="col-12"><label class="form-label">Debit account</label><select class="form-select" name="debit_account_id" required><option value="">Choose debit account</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected(old('debit_account_id')==$account->id)>{{ $account->code }} — {{ $account->head_name }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Credit account</label><select class="form-select" name="credit_account_id" required><option value="">Choose credit account</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected(old('credit_account_id')==$account->id)>{{ $account->code }} — {{ $account->head_name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Amount</label><input class="form-control" type="number" min=".01" step=".01" name="amount" value="{{ old('amount') }}" required></div><div class="col-md-6"><label class="form-label">Supplier (optional)</label><select class="form-select" name="supplier_id"><option value="">None</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}">{{ $supplier->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Customer (optional)</label><select class="form-select" name="customer_id"><option value="">None</option>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->full_name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Purchase (optional)</label><select class="form-select" name="purchase_id"><option value="">None</option>@foreach($purchases as $purchase)<option value="{{ $purchase->id }}">{{ $purchase->purchase_number }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Sale reference (optional)</label><input class="form-control" type="number" min="1" name="sale_id" value="{{ old('sale_id') }}" placeholder="Future sale ID"></div><div class="col-12"><label class="form-label">Narration</label><textarea class="form-control" rows="2" name="ledger_comment" placeholder="Why is this voucher being posted?">{{ old('ledger_comment') }}</textarea></div></div><button class="btn text-white mt-4" style="background:var(--brand)">Post balanced voucher</button></form></div><div class="col-xl-7"><div class="panel p-0 overflow-hidden"><div class="p-3 border-bottom"><strong>Recent ledger entries</strong></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Date</th><th>Voucher</th><th>Account</th><th>Debit</th><th>Credit</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td>{{ $transaction->transaction_date->format('d M Y') }}</td><td><code>{{ $transaction->voucher_no }}</code><br><small class="text-muted">{{ ucfirst($transaction->voucher_type) }}</small></td><td>{{ $transaction->account->code }} — {{ $transaction->account->head_name }}<br><small class="text-muted">{{ $transaction->ledger_comment }}</small></td><td>{{ $transaction->entry_type === 'debit' ? '$'.number_format($transaction->amount,2) : '—' }}</td><td>{{ $transaction->entry_type === 'credit' ? '$'.number_format($transaction->amount,2) : '—' }}</td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">No vouchers posted yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $transactions->links() }}</div></div></div>
@endsection
