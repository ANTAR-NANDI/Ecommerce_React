@extends('layouts.admin')
@section('title','Users & Roles')
@section('content')
<div class="d-flex justify-content-between align-items-center content-heading mb-4"><div><h1>Users & Roles</h1><p>Super Admin manages every warehouse. Warehouse Admin and Salesman accounts are limited to one warehouse.</p></div><a href="{{ route('admin.users.create') }}" class="btn text-white" style="background:var(--brand)"><i class="bi bi-person-plus me-1"></i>Add user</a></div><div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Warehouse</th><th>Created</th></tr></thead><tbody>@forelse($users as $user)<tr><td class="fw-semibold">{{ $user->name }}</td><td>{{ $user->email }}</td><td><span class="order-status {{ $user->role === 'superadmin' ? 'processing' : ($user->role === 'warehouse_admin' ? 'accepted' : 'shipped') }}">{{ str_replace('_',' ',$user->role) }}</span></td><td>{{ $user->warehouse?->name ?: 'All warehouses' }}</td><td>{{ $user->created_at->format('d M Y') }}</td></tr>@empty<tr><td colspan="5" class="text-center py-5">No users found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>
@endsection
